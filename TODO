# $Id$
# CPSDefault TODO

- CPS3 a la CMF
   o checking all wf and local role action
   o implement content_accept_publish_form (in_submit -> published | reject)
   o implement folder_factories_form
   o add wf_variable, transition - date - user
   o display more infos 'status' in folder_contents view
   o display history in publish_form
   o or make a document status page displaying all proxy rev lang status ...
   o add ptree infos
   o make sections and workspace box on left and actions box on right
   o put members folder in workspace root
   o clean cmf stuff ? undo ? pref ? fav ?
   o copy/cut/paste ...
   o make schema or sample doc for the 4 wf + navigation
   o finish zmi create CPSSite form
   o find utests with maximum coverage

- Bug fix
   o fix duplicate 'CPSDefault' and 'CPS Default' ptype name
   o filter sections in folder_contents sectionReader 
      can not access section with in_submit document
   o filter sections in content_publish_form display only section 
      with where we are SectionReader, using traveresall ?
   o how to displayed that a doc is freeze, no isFreezed method ?
   o folder_contents display links with broken path 
      if there are a missing trailing '/' in the URL

- Open questions
   o still need simple scenarii wf/multi version/multi language
   o unpublish what's happen to the history of a doc 
      how to remember that it was published and by who and when
   o how to accept multiple submit for a doc ?
   o how to allow unpublish for author

- CPS Pro
   o Use elements
   o Integration of * NuxCPS2 products


