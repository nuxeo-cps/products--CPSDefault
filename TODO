# $Id$
# SSS3 TODO

- CPS3 into CMF
   o override cps proxy folder actions (fti)
      to use cmf name instead of cps* action
   o finish submit page action plug section selection in publish
   o local roles impl
   o plug portal_trees into eventservice
   o rename SSS3 into CPSDefault
   o make sections and workspace box on left and actions box on right
   o put members folder in workspace root

- finish cpsinstall 
   o creation of account using use metadirectories
   o customization of zmi create page
   o permission, * cpsinstall V2

- use element
